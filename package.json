{
  "name": "bts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css:watch": "npx tailwindcss -i ./input.css -o ./public/output.css --watch",
    "css:build": "npx tailwindcss -i ./input.css -o ./public/output.css",
    "start": "http-server -d ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.5"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
